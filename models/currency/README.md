# Currency training artifacts (optional)

YOLO weights and `data.yaml` used by local training scripts (e.g. `train_currency.py`).  
`*.pt` files are gitignored; keep large binaries out of git or in object storage.
